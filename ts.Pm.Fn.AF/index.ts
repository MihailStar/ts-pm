// ⋕js.ts.Pm.Fn.AF.1
const func = (num1: number, num2: number): number => num1 + num2;

// ⋕js.ts.Pm.Fn.AF.2
const func2 = (str: string): string[] => str.split('');

// ⋕js.ts.Pm.Fn.AF.3
const arr: number[] = [1, 2, 3];
const res: number[] = arr.map((num: number): number => num ** 2);

console.log(res);
